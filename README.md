# Pi Extensions

A TypeScript monorepo for Pi Coding Agent extensions.

## Packages

- [`@shinku/pi-runtime-settings`](./packages/pi-runtime-settings): reports effective model and compaction values from the active session with `/runtime-settings`.

## Development

```sh
npm install
npm run check
```
