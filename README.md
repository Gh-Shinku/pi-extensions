# Pi Extensions

A TypeScript monorepo for Pi Coding Agent extensions.

## Packages

- [`@shinku/pi-compaction-budget`](./packages/pi-compaction-budget): reports the summarization output budget Pi really grants for `/compact`, together with the model and compaction values it resolved. See `/compact-budget`.

## Development

```sh
npm install
npm run check
```
